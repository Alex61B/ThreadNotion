node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Local Claude/Cursor worktrees can contain nested git repos
/.claude/worktrees/

# Local-only settings (keep shared settings in `.claude/settings.json`)
/.claude/settings.local.json
