import type { SalesSkill } from '../../schemas/coaching';
/**
 * Deterministic variant index for catalog rotation from a stable seed (userId, conv id, etc.).
 */
export declare function drillVariantIndexFromSeed(seed: string, skill: SalesSkill, salt?: string): number;
//# sourceMappingURL=drillVariations.d.ts.map